# CoverFi docs images

Place exported image assets for the Mintlify docs in this folder.

Current architecture image:

```text
coverfi-system-architecture.png
```

The interactive system architecture view in `coverfi-docs/investor-architecture.mdx` is rendered as Mermaid instead of using this PNG.

This image is referenced by:

- `INVESTOR_ARCHITECTURE_README.md`
